# dicas
- configure PATH, NODE_PATH, NODE_MODULES
	+ acrescentar ao `PATH` `npm -g prefix` para executar alguns comandos `.bat` ou `.cmd` dos módulos
	+ adicionar NODE_PATH apontando para `npm -g prefix` | `npm -g root`
	+ adicionar NODE_MODULES apontando para `npm -g prefix` | `npm -g root`
	